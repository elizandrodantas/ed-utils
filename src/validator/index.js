const { isArray, isBoolean, isFunction, isNumber, isObject, isString, isUndefined } = require('./typeof');

module.exports = {
    isArray,
    isBoolean,
    isFunction,
    isNumber,
    isObject,
    isString,
    isUndefined
}